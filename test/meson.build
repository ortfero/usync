project('usync-test', 'cpp',
  default_options: ['cpp_std=c++17'])

incdirs = include_directories('../include')

deps = []

executable('usync-test', 'test.cpp',
	include_directories: incdirs,
	dependencies: deps)
